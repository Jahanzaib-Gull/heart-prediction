# ❤️ CardioCheck --- Heart Disease Risk Prediction

CardioCheck is a **machine learning web application** built with
**Python and Streamlit**. It uses a trained **K-Nearest Neighbors
(KNN)** model to estimate heart disease risk from selected patient
clinical information.

> **Disclaimer:** This project is intended for educational and
> demonstration purposes only. The prediction is not a medical diagnosis
> and should not replace advice from a qualified healthcare
> professional.

## 🌐 Live Demo

After deployment, add your Streamlit Cloud URL here:

**Live App:** `https://your-app-name.streamlit.app`

## ✨ Features

-   ❤️ Clean, modern Streamlit interface
-   👤 Patient information input
-   🩺 Clinical health measurements
-   📊 Heart disease risk prediction using a KNN model
-   ⚙️ Automatic feature scaling
-   🔢 One-hot encoded categorical inputs
-   📈 Model probability display when supported
-   📱 Responsive layout
-   ⚠️ Medical-use disclaimer

## 🧠 Machine Learning Model

The application uses a trained **K-Nearest Neighbors (KNN)** classifier.

The model receives the following information:

  Feature           Description
  ----------------- ------------------------------------------------
  Age               Patient age
  Sex               Male / Female
  Chest Pain Type   Type of chest pain
  RestingBP         Resting blood pressure
  Cholesterol       Cholesterol level
  FastingBS         Whether fasting blood sugar is above 120 mg/dL
  RestingECG        Resting electrocardiogram result
  MaxHR             Maximum heart rate
  ExerciseAngina    Exercise-induced angina
  Oldpeak           ST depression
  ST_Slope          Slope of the ST segment

The application prepares the input using the same expected columns and
scaler used during model training before making a prediction.

## 📁 Project Structure

``` text
heart-prediction/
│
├── app.py
├── KNN_heart.pkl
├── scalar.pkl
├── columns.pkl
├── requirements.txt
└── README.md
```

### File Description

-   `app.py` --- Streamlit application
-   `KNN_heart.pkl` --- trained KNN model
-   `scalar.pkl` --- feature scaler
-   `columns.pkl` --- expected model input columns
-   `requirements.txt` --- Python dependencies
-   `README.md` --- project documentation

## 🛠️ Technologies Used

-   Python
-   Streamlit
-   Pandas
-   Scikit-learn
-   Joblib
-   K-Nearest Neighbors (KNN)

## 🚀 Run Locally

### 1. Clone the repository

``` bash
git clone https://github.com/YOUR-USERNAME/heart-prediction.git
cd heart-prediction
```

### 2. Install dependencies

``` bash
pip install -r requirements.txt
```

### 3. Run the Streamlit app

``` bash
streamlit run app.py
```

The application will open in your browser.

## ☁️ Deploy on Streamlit Community Cloud

1.  Upload all project files to GitHub.
2.  Open Streamlit Community Cloud.
3.  Connect your GitHub account.
4.  Select the `heart-prediction` repository.
5.  Select the `main` branch.
6.  Set the main file to:

``` text
app.py
```

7.  Click **Deploy**.

Make sure these model files are present in the repository:

``` text
KNN_heart.pkl
scalar.pkl
columns.pkl
```

## 📦 Requirements

The project requires:

``` text
streamlit
pandas
scikit-learn
joblib
```

For best compatibility, use the same `scikit-learn` version that was
used when the model was trained.

## ⚠️ Disclaimer

This application is an educational machine learning project. Its
predictions are generated by a statistical model and may be incorrect.

**Do not use this application to make medical decisions.** If you have
symptoms or concerns about heart health, consult a qualified healthcare
professional.

## 👨‍💻 Author

**Zaib**

Machine Learning & Streamlit Project

------------------------------------------------------------------------

⭐ If you find this project useful, consider giving the repository a
star!
